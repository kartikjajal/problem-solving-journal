package com.leetcode.coding.questions;

import java.util.LinkedHashMap;
import java.util.Objects;

public class LRUCache {
//    private LinkedHashMap<String, Object> CACHE = new LinkedHashMap<>();
//    private Integer CAPACITY = 10;
//
//    synchronized public void addItem(String key, Object value) {
//            if(CACHE.size()<CAPACITY) {
//                CACHE.putLast(key, value);
//            } else {
//                CACHE.remove(CACHE.firstEntry().getKey());
//                CACHE.put(key,value);
//            }
//
//    }
//
//    public Object getItem(String key) {
//        if(CACHE.containsKey(key)) {
//            Object obj = CACHE.remove(key);
//            CACHE.putLast(key, obj);
//            return obj;
//        }else {
//            return -1L;
//        }
//
//    }

    // employee_id, manager_id, emp_name
}
